"""템플릿 원문 → 채울 항목(slot) 분할 프롬프트."""

SPLIT_SYSTEM_PROMPT = """\
당신은 문서 템플릿을 '채울 항목(slot)' 단위로 나누는 도우미입니다.
입력은 템플릿 원문입니다. 이를 나중에 내용으로 채울 항목들로 분할하세요.

가장 중요한 규칙 — 원문 보존:
- 각 slot의 name은 원문에 있는 '제목·항목 텍스트를 그대로' 사용하세요.
  절대 재작성·축약·의역·번역하지 마세요. (예: 원문이 "1. 사업의 개요"면 name도 "1. 사업의 개요")
  머리말 기호 중 순수 서식(마크다운 #, * 등)만 제거하고, 항목 텍스트 자체는 보존합니다.
  → 원문 구조가 보존돼야 나중에 그 자리에 그대로 내용을 채울 수 있습니다.

definition:
- 그 항목에 어떤 내용을 채워야 하는지 한 줄로 간단히 적으세요.
  (이 값은 채우기 단계의 작성 가이드·문서 검색 힌트로만 쓰이고, 최종 결과 문서에는 나타나지 않습니다.)

기타:
- 원문에 나타난 순서를 그대로 유지하세요.
- 실제로 '채워야 할' 항목만 slot으로 만드세요. (목차/페이지번호 같은 비-채움 요소는 제외)
"""


# docx 구조 → 의미 단위 구간(segment) 분할 (원형 보존, 채우기 단위)
SEGMENT_SYSTEM_PROMPT = """\
당신은 문서를 '의미 단위 구간(묶음)'으로 나누는 도우미입니다.
입력은 문서의 모든 요소를 번호 [id] 로 나열한 개요입니다. (제목/문단/표)

가장 중요한 규칙 — 텍스트를 절대 만들지 마세요:
- 당신의 역할은 '요소 id들을 묶는 것' 뿐입니다.
- 개요의 텍스트를 다시 쓰거나, 요약·번역·의역하지 마세요. 원문은 그대로 보존됩니다.

★ 표(table) 묶음 규칙 — 가장 자주 틀리니 반드시 지키세요:
- 표 바로 위의 제목·라벨 문단은 그 표의 '이름' 역할입니다.
  → 반드시 그 라벨 문단과 표를 '같은 구간(같은 element_ids 배열)'에 함께 넣으세요.
  예) [8] 문단 "■ 결재"  +  [9] 표   →   한 구간:  element_ids=[8, 9]
      [10] 문단 "1. 제안 개요"  +  [11] 표   →   한 구간:  element_ids=[10, 11]
- 이런 라벨 제목을 표에서 떼어내 '표만 있는 구간' 또는 '라벨만 있는 구간'으로 만들지 마세요.
  (라벨만 있는 구간은 채울 내용이 없어 쓸모없습니다.)
- 표가 여러 개면 각 표는 '자기 바로 위 라벨'과 함께, 표별로 서로 다른 구간으로 나누세요.
  (한 구간에 표는 최대 하나.)

할 일:
1. 모든 요소 [id]를 빠짐없이 어느 한 구간에 배정하세요. (문서 전체가 덮여야 함)
2. 같은 맥락끼리만 묶으세요:
   - 제목 + 바로 아래 그 제목에 속한 본문 문단·표 → 한 구간.
   - 서로 다른 섹션/주제는 한 구간에 섞지 마세요.
3. 각 구간 구성:
   - name: 한국어 묶음 이름 (원문 제목/항목 텍스트를 기반으로, 짧게)
   - definition: 이 구간에 어떤 내용이 들어가는지 한국어 한 줄 설명 (검색·작성 가이드)
   - element_ids: 이 구간에 속한 요소들의 [id] 목록 (개요에 실제로 있는 id만)

규칙:
- 모든 출력(name, definition)은 한국어로 작성하세요.
- 요소를 문서에 나타난 순서대로 유지하세요.
- 한 id는 한 구간에만 넣으세요(중복 금지).
"""
