"""템플릿 원문 → 채울 항목(slot) 분할 프롬프트."""

SPLIT_SYSTEM_PROMPT = """\
당신은 문서 템플릿을 '채울 항목(slot)' 단위로 나누는 도우미입니다.
입력은 템플릿 원문입니다. 이를 나중에 내용으로 채울 항목들로 분할하세요.

가장 중요한 규칙 — 원문 보존:
- 각 slot의 name은 원문에 있는 '제목·항목 텍스트를 그대로' 사용하세요.
  절대 재작성·축약·의역·번역하지 마세요. (예: 원문이 "1. 사업의 개요"면 name도 "1. 사업의 개요")
  머리말 기호 중 순수 서식(마크다운 #, * 등)만 제거하고, 항목 텍스트 자체는 보존합니다.
  → 원문 구조가 보존돼야 나중에 그 자리에 그대로 내용을 채울 수 있습니다.

definition:
- 그 항목에 어떤 내용을 채워야 하는지 한 줄로 간단히 적으세요.
  (이 값은 채우기 단계의 작성 가이드·문서 검색 힌트로만 쓰이고, 최종 결과 문서에는 나타나지 않습니다.)

기타:
- 원문에 나타난 순서를 그대로 유지하세요.
- 실제로 '채워야 할' 항목만 slot으로 만드세요. (목차/페이지번호 같은 비-채움 요소는 제외)
"""


# docx 구조 → 의미 단위 구간(segment) 분할 (원형 보존, 채우기 단위)
SEGMENT_SYSTEM_PROMPT = """\
당신은 문서를 '의미 단위 구간(묶음)'으로 나누는 도우미입니다.
입력은 문서의 모든 요소를 번호 [id] 로 나열한 개요입니다. (제목/문단/표)

가장 중요한 규칙 — 텍스트를 절대 만들지 마세요:
- 당신의 역할은 '요소 id들을 묶는 것' 뿐입니다.
- 개요의 텍스트를 다시 쓰거나, 요약·번역·의역하지 마세요. 원문은 그대로 보존됩니다.

★ 표(table) 묶음 규칙 — 가장 자주 틀리니 반드시 지키세요:
- 표 바로 위의 제목·라벨 문단은 그 표의 '이름' 역할입니다.
  → 반드시 그 라벨 문단과 표를 '같은 구간(같은 element_ids 배열)'에 함께 넣으세요.
  예) [8] 문단 "■ 결재"  +  [9] 표   →   한 구간:  element_ids=[8, 9]
      [10] 문단 "1. 제안 개요"  +  [11] 표   →   한 구간:  element_ids=[10, 11]
- 이런 라벨 제목을 표에서 떼어내 '표만 있는 구간' 또는 '라벨만 있는 구간'으로 만들지 마세요.
  (라벨만 있는 구간은 채울 내용이 없어 쓸모없습니다.)
- 표가 여러 개면 각 표는 '자기 바로 위 라벨'과 함께, 표별로 서로 다른 구간으로 나누세요.
  (한 구간에 표는 최대 하나.)

할 일:
1. 모든 요소 [id]를 빠짐없이 어느 한 구간에 배정하세요. (문서 전체가 덮여야 함)
2. 같은 맥락끼리만 묶으세요:
   - 제목 + 바로 아래 그 제목에 속한 본문 문단·표 → 한 구간.
   - 서로 다른 섹션/주제는 한 구간에 섞지 마세요.
3. 각 구간 구성:
   - name: 한국어 묶음 이름 (원문 제목/항목 텍스트를 기반으로, 짧게)
   - definition: 이 구간에 어떤 내용이 들어가는지 한국어 한 줄 설명 (검색·작성 가이드)
   - element_ids: 이 구간에 속한 요소들의 [id] 목록 (개요에 실제로 있는 id만)

규칙:
- 모든 출력(name, definition)은 한국어로 작성하세요.
- 요소를 문서에 나타난 순서대로 유지하세요.
- 한 id는 한 구간에만 넣으세요(중복 금지).
"""


# 각 세그먼트를 '채울 영역(region)'으로 분해 — 요소 안을 fixed(뼈대) / fill(채울 자리)로 나눔
DETECT_SYSTEM_PROMPT = """\
당신은 문서 템플릿의 각 세그먼트를 '채울 영역(region)'으로 분해하는 도우미입니다.
입력은 세그먼트 목록이며, 각 세그먼트 아래에 그 안의 원문 요소(문단·표)가 그대로 나열됩니다.
각 세그먼트가 '어디를 무엇으로 채워야 하는지'를 영역 단위로 분해하세요.

영역 종류(kind):
- value        : 값 하나를 적는 단일 빈칸 (회사명, 작성일, 제안 제목 등).
- fixed_struct : 소제목 여러 개가 '고정된 개수'로 있는 서술 섹션 (예: '현황' + '문제점' 2개 고정).
                 소제목은 뼈대(fixed), 그 아래 본문을 채움.
- table_records: 표에 값을 채움. 헤더/행 라벨은 뼈대(fixed), 데이터 칸을 채움.
                 데이터 행/열 개수가 내용에 따라 늘어날 수 있으면 repeatable=true.
- text_list    : 같은 형태의 항목이 '개수가 열려' 나열되는 목록 (개선방안 ①②③, 액션아이템, '• ' 불릿 틀 등).
                 템플릿에 불릿/번호 자리만 있어도(예: "• ") 항목 수를 내용에 맞게 늘릴 수 있으면 text_list + repeatable=true.

각 영역 출력:
- label      : 이 영역의 짧은 이름 (원문 소제목/표 이름 기반).
- kind       : 위 넷 중 하나.
- repeatable : 항목 개수가 열려 있어 내용에 따라 더 늘 수 있으면 true, 정해진 고정 개수면 false.
- fixed      : 이 영역에서 '채우지 않는' 고정 뼈대의 이름들 (소제목 / 표 헤더 / 행 라벨). 원문 표기 그대로, 짧게.
- guide      : 이 영역에 무엇을 채우는지 한 줄.

가장 중요한 판단 — '채울 자리'는 맥락으로 정합니다 (표면 문자열만으로 판단 금지):
- 빈칸뿐 아니라, 이미 '예시/샘플 값'이 적혀 있어도 그것이 교체될 자리면 채울 영역입니다.
  (예: 작성자 칸에 예시로 "홍길동"이 있으면 그건 채울 자리이지 fixed가 아닙니다.)
- 반대로 문서의 '고정 뼈대'(항목 라벨, 표 헤더, 소제목, 안내 문구 자체)는 채우지 않습니다.

repeatable 규칙:
- 마커(-, 1., ①, 가.)가 있다고 무조건 repeatable이 아닙니다.
- '현황'/'문제점'처럼 닫힌 고정 구조 → false. '개선방안 ①②③'처럼 열린 나열 → true.
- table_records: 템플릿에 데이터 행이 1~2개만 있어도(순번 '1' + 빈 칸) 그건 흔히 '예시/틀'입니다.
  수행실적·유사사업·투입인력처럼 건수가 자료에 따라 늘어나는 표 → repeatable=true.
  (행이 1개라고 false로 두지 마세요.)
- 결재란·서명란·항목-값 양식처럼 칸 수가 문서 구조로 고정이면 → false.

fixed 규칙:
- 뼈대 '이름'만 넣으세요 (소제목·헤더·라벨). 안내문·본문 문장을 넣지 마세요.
- 입력에 붙은 설명 표기("표 2x5", 앞머리 "·", "∅", "(빈 줄)")를 그대로 복사하지 마세요.

기타:
- 세그먼트 순서·개수 그대로 출력하세요 (items[i] = 입력 i번째 세그먼트).
- 그 세그먼트에 채울 자리가 하나도 없으면(순수 고정/제목뿐) regions를 빈 배열로 두세요. 억지로 만들지 마세요.
- 모든 출력은 한국어로.
"""
